kind: Gateway
apiVersion: gateway.networking.k8s.io/v1
metadata:
  name: super-gateway
spec:
  gatewayClassName: agentgateway
  listeners:
  - protocol: HTTP
    port: 8080
    name: http
    allowedRoutes:
      namespaces:
        from: All