[project]
name = "echo-agent"
version = "0.1.0"
dependencies = [
    "click",
    "a2a-samples @ git+https://github.com/google/A2A@main#subdirectory=samples/python"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
